<span>
  <div id="main-content">
    <h1>Flashcard</h1>
    <div  class="content">
      <%= image_tag("https://res.cloudinary.com/flashcard/image/upload/v1614949362/Landing-page%20background%20and%20logo/new-orange-logo_siahlm.png", :type => "fetch", class:"logo") %>
      <div class="links">
        <% if user_signed_in? %>
          <h3>Welcome back, <%= (current_user.first_name).capitalize %></h3>
          <h6>You're Last Study Session: <%= current_user.last_sign_in_at.strftime("%A, %d %B") %></h6>
          <%= link_to "My Dashboard", dashboard_path, class:"btn btn-success", id:"log-in" %>
        <% else %>
          <h3>Start your revision with Flashcard, the modern way to study using flashcards. </h3>
          <%= link_to "Get started today!" , new_user_registration_path ,class:"btn btn-success", id:"sign-up" %>
          <%= link_to "Log-In", new_user_session_path ,class:"btn btn-primary", id:"log-in" %>
        <% end %>
      </div>
    </div>
  </div>
</span>
